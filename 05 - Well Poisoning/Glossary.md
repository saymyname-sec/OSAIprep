# Module 05 — Glossary

**AML.T0051.001 — LLM Prompt Injection: Indirect**
MITRE ATLAS classification for retrieval hijacking: a poisoned document retrieved from the knowledge base acts as the injection vector. The attacker never touches the user input — the malicious instruction arrives via the retrieval process.

**AML.T0057 — LLM Data Leakage**
MITRE ATLAS classification for knowledge base probing: extracting sensitive data (credentials, hostnames, API keys) by querying an LLM that has access to internal documents via RAG.

**AML.T0070 — RAG Poisoning**
MITRE ATLAS classification for ingestion poisoning and embedding collision: deliberately crafting documents to corrupt the knowledge base so future retrievals return attacker-controlled content.

**Arize Phoenix**
Observability platform (runs on port 6006) that traces every RAG event — ingestion previews, chunk boundaries, retrieval scores, tool calls, LLM inference. Used by defenders to diagnose issues; by attackers to verify stealth (ensure payload doesn't appear in chunk preview).

**Augmented Prompt**
The final prompt sent to the LLM: system prompt + retrieved chunks + user query. The LLM cannot distinguish between retrieved chunks and user input — both are trusted context. This is the core exploitable assumption in RAG attacks.

**BM25**
Probabilistic keyword ranking algorithm used in OpenSearch for exact-match retrieval. Scores based on term frequency and inverse document frequency. Complements vector search in hybrid retrieval by catching exact keyword matches that semantic search may miss.

**Chunk**
A fixed-size text fragment created during ingestion (in this lab: 800 chars with 200-char overlap). The basic unit of storage and retrieval. Each chunk is embedded independently, indexed in BM25 independently, and stored in PostgreSQL independently.

**Chunk Overlap**
The portion shared between adjacent chunks (200 chars in this lab). Prevents information from being lost at chunk boundaries. Also means a payload placed near the end of a chunk appears in the next chunk's beginning — doubling detection risk if placed in the overlap zone.

**Contextual Activation (RAG variant)**
A poisoned document written to activate only for specific query types — the payload contains a condition ("when this product appears in a Q4 executive summary") so it fires only for targeted queries and doesn't affect general user traffic.

**Cosine Similarity**
Mathematical measure of angle between two vectors (range: −1 to 1; typically 0 to 1 for embeddings). Weaviate ranks retrieved chunks by cosine similarity to the query vector. Higher cosine similarity = semantically closer to the query.

**Cyrillic Homoglyph**
A Cyrillic Unicode character visually identical to a Latin ASCII character (e → е U+0435, a → а U+0430, p → р U+0440). Used to bypass string-matching filters: the byte representation differs, so regex fails to match; the visual representation is identical, so human reviewers don't notice.

**Dense Vector**
A fixed-length array of floating-point numbers representing the semantic meaning of text. Generated by the embedding model (`sentence-transformers/all-MiniLM-L6-v2` in this lab). Chunks with similar meaning have vectors that are geometrically close (high cosine similarity).

**Document Blending**
Evasion technique: make a poisoned document indistinguishable from legitimate documents by matching file format, naming convention, writing tone, and structural position of the payload (past the preview boundary). Evades human review based on truncated previews; does NOT evade full-document scanning.

**Embedding Collision**
Attack where a single document covers many topics so its chunk embeddings land near the centroid of multiple semantic spaces — retrieved for ANY query matching any of those topics. One document, payload in every chunk, triggers on any employee query.

**Embedding Model**
Neural network that converts text to a dense vector. In this lab: `sentence-transformers/all-MiniLM-L6-v2`. The SAME model is used for both ingestion (chunks → vectors) and retrieval (query → vector). Critical: if you can identify the embedding model, you can compute exact cosine similarities to craft collision documents.

**Event Fatigue**
Slow-drip poisoning tactic: flood the system with many benign ingestion events to bury the meaningful (malicious) ones in noise. Defenders reviewing the monitoring dashboard see hundreds of normal events and miss the few that matter.

**Hybrid Retrieval**
Combining BM25 keyword search (OpenSearch) and vector similarity search (Weaviate) for a single query. Scores are normalized to the same scale and fused via weighted averaging. Doubles the attacker's retrieval surface — a poisoned document can be retrieved via either keyword match OR semantic similarity.

**Ingestion Poisoning**
Uploading a document with hidden LLM instructions that execute whenever the document is retrieved. The simplest RAG attack — requires only file upload access. MITRE: AML.T0070.

**Input Guardrail**
Security filter applied to user input BEFORE it reaches the retriever or LLM. Blocks injection keywords, malicious file paths (`/etc/passwd`), and policy violations. Key bypass: move malicious content into an uploaded document — input guardrails don't filter retrieved context.

**Knowledge Base (KB)**
The collection of documents indexed by the RAG system. In this lab: triple-stored per chunk (PostgreSQL metadata, Weaviate vectors, OpenSearch BM25). If an attacker can write to the KB, they control what the LLM knows and how it responds — without modifying the model.

**Multi-Modal Camouflage**
Hiding instructions in non-text content that LLMs can process but humans don't read: white text on white background in PDFs, instructions in image alt-text, payloads in PDF metadata fields, OCR-readable text in images. The LLM extracts and acts on the hidden content; human reviewers see only the visual surface.

**OpenSearch**
Elasticsearch-compatible search engine used for BM25 keyword indexing of raw chunk text. Paired with Weaviate for hybrid retrieval. Stores raw text (not embeddings) and returns results based on term frequency statistics.

**Output Guardrail**
Security filter applied to LLM-generated response BEFORE it reaches the user. Implements PII redaction (emails, SSNs, IPs), safety checks, and policy compliance using regex, NER, and sometimes secondary LLM calls. Bypass: instruct the LLM to format output in a pattern the filter regex won't match.

**PostgreSQL (in RAG context)**
Relational database storing document metadata and chunk records. Maps each chunk to its Weaviate UUID and OpenSearch document ID. Enables access control (filter by document permissions), metadata filtering (by language, date, type), and source attribution.

**Prompt Decomposition**
The distributed poisoning strategy applied to RAG: split a restricted instruction (e.g., "read /etc/passwd") into harmless fragments across multiple documents. Each fragment is individually innocuous — the complete instruction only assembles in the model's context window when both fragments are retrieved together.

**RAG (Retrieval-Augmented Generation)**
Architecture that extends LLMs with external knowledge by retrieving relevant documents at inference time and injecting them into the prompt. Solves the training data cutoff problem without retraining. Attack surface: the retrieval pipeline and knowledge base.

**Reasoning Loop**
Iterative RAG behaviour: retrieve → generate partial answer → check against retrieved content → reformulate query → retrieve again → final answer. Improves accuracy and catches hallucinations. Attacker implications: more queries = more chances to trigger poisoned documents; loop may also catch inconsistencies in poisoned content.

**Retrieval Hijacking**
Embedding LLM tool-call instructions in an uploaded document that only triggers on specific (attacker-controlled) queries. The LLM executes the instructions (e.g., read a file) as trusted retrieved context, bypassing input filters. MITRE: AML.T0051.001.

**Sentence-Transformers / all-MiniLM-L6-v2**
Open-source embedding model used in this lab. Produces 384-dimensional dense vectors. Available locally, no API key needed. Knowing the embedding model allows computing exact cosine similarities to engineer optimal collision documents.

**SHA-256 Hash (ingestion)**
Hash computed per document during ingestion. Used to detect changes — documents with unchanged hashes are skipped; only new or modified files are re-processed. Means uploading a modified version of a document always triggers full re-ingestion (removes old embeddings, inserts new ones).

**Slow-Drip Poisoning**
Temporal variant of distributed poisoning: introduce each malicious fragment via a small, individually-innocuous edit spaced hours or weeks apart. Change-review tools see only one small edit at a time, never the full picture. Revert all fragments after triggering to erase forensic traces.

**Token Smuggling**
Technique of embedding fake model-specific special tokens (`<|im_start|>system`) or hyphenated instruction strings (`I-g-n-o-r-e`) in poisoned documents to manipulate how the model interprets message boundaries or instruction hierarchy. Highly model-dependent; most effective against smaller, instruction-tuned models.

**Top-K Selection**
Choosing the K most relevant chunks (K=6 in this lab) after hybrid retrieval and score fusion. The LLM only sees these 6 chunks — not the full knowledge base. Attack implication: a poisoned document must rank in the top 6 for the target query, or it won't be retrieved and the payload won't fire.

**vLLM**
Open-source inference server for hosting LLMs efficiently. Handles GPU batching and optimization. In this lab: serves Qwen3.5-35B-A3B-FP8 on a local inference endpoint. The LLM that processes the augmented prompt and generates responses.

**Weaviate**
Vector database storing chunk embeddings and metadata. Performs cosine similarity search when given a query vector. Returns UUID-identified chunks that are then looked up in PostgreSQL for metadata. Primary semantic retrieval layer in the hybrid search stack.

**Zero-Width Space (U+200B)**
Unicode character with no visual glyph, no width, and no effect on text rendering. Inserted between characters of a sensitive string (e.g., `/etc/passwd`) to break keyword regex matching at the byte level. The LLM tokeniser processes the surrounding characters and reconstructs semantic meaning. Visible as markers (dots/boxes) in monitoring tools like Phoenix.
